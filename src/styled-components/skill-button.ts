import styled from '@emotion/styled'
import { Button } from '@chakra-ui/react'

const SkillButton = styled(Button)({
  height: '1.5rem',
  width: '5rem',
  borderRadius: 0,
})

export { SkillButton }
