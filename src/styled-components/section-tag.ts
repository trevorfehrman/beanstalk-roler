import styled from '@emotion/styled'
import { Tag } from '@chakra-ui/react'

const SectionTag = styled(Tag)({
  backgroundColor: '#ffb549',
  fontSize: '1.3rem',
  borderBottomLeftRadius: 0,
  borderBottomRightRadius: 0,
  color: '#2e2e2e',
})

export { SectionTag }
